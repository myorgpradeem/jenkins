pipeline {
    agent ubuntu_slave 
    stages {
        stage ('build') {
            steps {
                sh "uptime"
            }
        }
        stage('archive') {
            steps {
                sh "pwd"
            }
        }
        stage('release') {
            steps {
                sh "free -m"
            }
        }
    }
}
